---

- name: Check OS version and family
  when: not skip_os_check
  tags: always
  ansible.builtin.assert:
    that: (ansible_facts.distribution == 'Amazon' and ansible_facts.distribution_major_version is version_compare('2023', '=='))
    fail_msg: "This role can only be run against Supported OSs. {{ ansible_facts.distribution }} {{ ansible_facts.distribution_major_version }} is not supported."
    success_msg: "This role is running against a supported OS {{ ansible_facts.distribution }} {{ ansible_facts.distribution_major_version }}"

- name: Check ansible version
  tags: always
  ansible.builtin.assert:
    that: ansible_version.full is version_compare(min_ansible_version, '>=')
    fail_msg: You must use Ansible {{ min_ansible_version }} or greater
    success_msg: This role is running a supported version of ansible {{ ansible_version.full }} >= {{ min_ansible_version }}

- name: Setup rules if container
  when: ansible_connection == 'docker' or ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"]
  tags:
    - container_discovery
    - always
  block:
    - name: Discover and set container variable if required
      ansible.builtin.set_fact:
        system_is_container: true

    - name: Load variable for container
      ansible.builtin.include_vars:
        file: "{{ container_vars_file }}"

    - name: Output if discovered is a container
      when: system_is_container
      ansible.builtin.debug:
        msg: system has been discovered as a container

- name: Gather the package facts
  tags: always
  ansible.builtin.package_facts:
    manager: auto

- name: Run prelim tasks
  tags:
    - prelim_tasks
    - run_audit
    - always
  ansible.builtin.import_tasks:
    file: prelim.yml

- name: Import CAT 1 tasks
  when: az2023stig_cat1
  ansible.builtin.import_tasks:
    file: cat_1/main.yml

- name: Import CAT 2 tasks
  when: az2023stig_cat2
  ansible.builtin.import_tasks:
    file: cat_2/main.yml

- name: Import CAT 3 tasks
  when: az2023stig_cat3
  ansible.builtin.import_tasks:
    file: cat_3/main.yml

- name: Import post tasks
  ansible.builtin.import_tasks:
    file: post.yml

- name: Run post remediation audit
  when: run_audit
  tags: run_audit
  ansible.builtin.import_tasks:
    file: post_remediation_audit.yml

- name: Add ansible file showing Benchmark and levels applied if audit details not present
  when:
    - create_benchmark_facts
    - (post_audit_json_summary is defined or post_audit_doc_summary is defined) or
      (ansible_local['compliance_facts']['lockdown_audit_details']['audit_summary'] is undefined and post_audit_json_summary is undefined and post_audit_doc_summary is undefined)
  tags:
    - always
    - benchmark
  block:
    - name: Create ansible facts directory if audit facts not present
      ansible.builtin.file:
        path: "{{ ansible_facts_path }}"
        state: directory
        owner: root
        group: root
        mode: 'u=rwx,go=rx'

    - name: Create ansible facts file and levels applied if audit facts not present
      ansible.builtin.template:
        src: etc/ansible/compliance_facts.j2
        dest: "{{ ansible_facts_path }}/compliance_facts.fact"
        owner: root
        group: root
        mode: 'u-x,go=r'

- name: Fetch audit files
  when:
    - fetch_audit_output
    - run_audit
  tags: always
  ansible.builtin.import_tasks:
    file: fetch_audit_output.yml

- name: Show Audit Summary
  when: run_audit
  tags: run_audit
  ansible.builtin.debug:
    msg: "{{ audit_results.split('\n') }}"

- name: If Warnings found Output count and control IDs affected
  when: warn_count != 0
  tags: always
  ansible.builtin.debug:
    msg: You have {{ warn_count }} Warning(s) that require investigating that are related to the following benchmark ID(s) {{ warn_control_list }}
